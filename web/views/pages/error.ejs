<%- include('../partials/header', { user }) %>

<div class="container mx-auto px-6 py-12">
  <div class="max-w-2xl mx-auto text-center">
    <div class="slide-up">
      <div class="text-9xl mb-8">🚫</div>
      <h1 class="text-5xl font-black mb-6 gold-text"><%= error || 'Error' %></h1>
      <p class="text-xl text-gray-400 mb-12"><%= message || 'Something went wrong.' %></p>
      
      <div class="flex justify-center space-x-4">
        <a href="/dashboard" class="px-8 py-4 gold-gradient text-black font-bold text-lg rounded-xl hover:opacity-90 transition">
          Go to Dashboard
        </a>
        <a href="/" class="px-8 py-4 glass-card text-white font-bold text-lg rounded-xl hover:bg-gray-800 transition">
          Back to Home
        </a>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
